---
layout: null
---

<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.name | xml_escape }}</title>
  <link href="{{ site.url | xml_escape }}/atom.xml" rel="self" />
  <link href="{{ site.url | xml_escape }}/"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url | xml_escape }}</id>
  <author>
    <name>{{ site.author.name }}</name>
    <email>{{ site.author.email }}</email>
  </author>
  {% for post in site.posts %}
    <entry>
      <title>{{ post.title }}</title>
      <link href="{{ site.url }}{{ post.url }}" />
      <updated>{{ post.date | date_to_xmlschema }}</updated>
      <id>{{ site.url | xml_escape }}{{ post.id }}</id>
      <content type="html">
        {% if post.image %}
          <![CDATA[ <img src="{{ site.baseurl }}/images/blog-images/{{ post.image }}" alt="{{ post.title }}" class="thumbnail"> ]]>
        {% endif %}
        {% if post.excerpt %}
          <p>{{ post.excerpt | remove: '<p>' | remove: '</p>' | xml_escape | strip_newlines }}..</p>
        {% else %}
          {{ post.content | xml_escape }}
        {% endif %}
      </content>
    </entry>
  {% endfor %}
</feed>